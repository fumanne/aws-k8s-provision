---

- name: Set Path
  hosts: all
  tasks:
    - name:  Modify /etc/environment
      lineinfile:
        path: /etc/environment
        regexp: '^export -p PATH'
        line: 'export -p PATH=$PATH:/root/local/bin/'
        owner: root
        group: root
        mode: 0644