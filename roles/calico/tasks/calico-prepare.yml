---
- name: calico cni 相关目录
  file: name={{ item }} state=directory
  with_items:
  - /root/local/calico

- name: scp calico yaml
  copy: src=calico.yaml dest=/root/local/calico/calico.yaml mode=0755

- name: scp calicoctl
  copy: src=calicoctl  dest=/root/local/bin/calicoctl mode=0755
